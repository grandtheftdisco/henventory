<% if notice.present? %>
  <p class="py-2 px-3 bg-blue-100 mb-5 text-green-700 font-medium inline-block rounded-2xl" id="notice"><%= notice %></p>
<% end %>

<% content_for :title, "New Collection Entry" %>

<h1 class="font-extrabold text-3xl text-blue-600 my-5 uppercase">New Collection Entry</h1>

<%= render "form_container", collection_entry: @collection_entry %>