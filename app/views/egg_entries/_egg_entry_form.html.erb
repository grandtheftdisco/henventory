<%= form_with(model: egg_entries) do |form| %>
  <% if egg_entry.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(egg_entry.errors.count, "error") %>       prohibited this egg entry from being saved:</h2>

      <ul>
        <% egg_entry.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>Egg Entry 1</p>
  <input name="egg_entries_attributes[][chicken_id]"/>
  <input name="egg_entries_attributes[][egg_count]"/>

  <p>Egg Entry 2</p>
  <input name="egg_entries_attributes[][chicken_id]"/>
  <input name="egg_entries_attributes[][egg_count]"/>

<!--# egg_entries_attributes: [{chicken_id: 2, egg_count: 1,
 id: 1}]-->

  <div>
    <%= form.submit %>
  </div>
<% end %>

<!-- code that was in the CE form -->

<h4>EGG ENTRY</h4>

      <h5>Chicken: </h5>
      <select name="collection_entry[egg_entries_attributes][][chicken_id]">

        <!-- form.collection_select :chicken_id, Chicken.all, :id, :name, prompt: true -->
        <% @chickens.each do |chicken| %>
          <option value="<%= chicken.id %>"><%= chicken.name %></option>
        <% end %>
      </select><br/><br/>

      <h5>Egg Count</h5>
      <input name="collection_entry[egg_entries_attributes][][egg_count]"/><br/><br/>