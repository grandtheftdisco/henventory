<div id="<%= dom_id chicken %>" class="w-1/2 justify-items-center justify-self-center">
  <h3 class="text-blue-400 font-extrabold text-5xl"><%= chicken.name %></h3><br/>

  <%= image_tag("#{chicken.image_url}", width: 400, class: "justify-self-center") %>
  <br/>
  <div class="text-left">
    <p>
      <strong>Breed:</strong>
      <%= chicken.breed %>
    </p>

    <p>
      <strong>DOB:</strong>
      <%= chicken.dob.strftime("%m/%d/%Y") %>
    </p>

    <p>
      <strong>Tell:</strong>
      <%= chicken.tell %>
    </p>

    <p><strong>egg count:</strong> display running total of chicken egg count here; over time average weekly egg count will also be implemented</p>
  </div>

</div>
