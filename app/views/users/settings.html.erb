<% if notice.present? %>
  <p class="py-2 px-3 bg-blue-100 mb-5 text-green-700 font-medium inline-block rounded-2xl" id="notice"><%= notice %></p>
<% end %>
  
<h1 class="font-extrabold text-3xl text-blue-800 mb-1">Settings</h1>
<p class="text-sm italic mb-2">Logged in as <%= @user.display_name %></p>

<div class="flex flex-col gap-4 border-1 border-blue-300 rounded-2xl bg-blue-100 text-l text-blue-950 justify-self-center justify-items-center md:shrink-0 w-100 md:w-150 py-5 px-3">
  <div><button class="font-bold text-white bg-blue-500 hover:bg-blue-600 p-3 rounded-2xl w-70"><%= link_to "user settings", edit_user_path(Current.user) %></button></div> 

  <div><button class="font-bold text-white bg-amber-600 hover:bg-amber-700 p-3 rounded-2xl w-70"><%= link_to "change your password", edit_password_path(Current.user.password_reset_token) %></button></div>

  <div class="bg-blue-50 border-1 border-blue-200 rounded-2xl mt-4 mx-2">
    <p class="font-bold mb-5">Add Someone To Your Household</p>
    <input type="text" value="<%= new_household_user_url(@household) %>" data-household-token="<%= @household.invite_token %>" size="50"/>
    <div class="py-5">
      <button class="bg-yellow-100 rounded-2xl">regen [PH]</button>
      <button class="bg-green-100 rounded-2xl">copy [PH]</button>
    </div>
  </div>

  <% if @expired_chickens.exists? %>
    <div class="mt-5">
      <%= link_to "🪦 View Expired Chickens", chickens_path(expired: true), class: "font-semibold hover:text-blue-800" %>
    </div>
  <% end %>
</div>
